import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <span>memento</span>
            <span>it's your time</span>
        </header>
    );
}

export default Header;